# 待修改（Secure SDLC 計畫）

## 目標
建立一個統一 Web 應用「SAST Studio」，把 Semgrep、Bearer、Trivy、npm audit、OSV-Scanner、Gitleaks 六個安全掃描工具整合成前端 UI + 後端 orchestrator，讓使用者一鍵對程式碼跑多工具靜態安全檢查並看彙整報表。

## 本輪：掃描器「沒分析卻回報 ok」— 根因與偵測（2026-09-25，第 41 輪）

使用者需求：查出 bearer 少報根因，讓「沒分析」不再顯示 ok；並確認其他掃描器是否有同樣問題（talk.md #020）。

### 根因（容器內實測，非推論）
- bearer：單檔分析有內部期限（約 30 秒，2.1.1 無參數可調，`--parallel` 無效）；超時就略過該檔，**只在 debug log** 寫 `failed to scan file …: context deadline exceeded`，rc 0。nessus-report 的 app.js（99 KB）在 2 核 VM 上正好卡在期限邊緣 → 有時 10 項、有時 1 項、有時 0 項，皆顯示 ok
- 同一份程式碼 git 掃描 1 項／ZIP 掃描 10 項的差異，來源就是這個不穩定，不是 git 或目錄結構（三種配置單獨跑都一樣）

### 六個掃描器逐一實測
| 工具 | 情境 | 修正前 | 修正後 |
|---|---|---|---|
| bearer | 檔案超時 | ok、少報 | incomplete + 檔名與原因；重試一次 |
| semgrep | 規則超時（errors 陣列） | ok | incomplete |
| trivy | lockfile 無法解析（只在 debug 有 Walk error；--quiet 連它都關） | ok、0 項 | incomplete（改 --debug） |
| osv-scanner | 部分 lockfile 無法解析（rc 127 + 部分 JSON） | ok | incomplete |
| osv-scanner | 全部 lockfile 無法解析 | not_applicable + 說明 | 不變（既有決議） |
| gitleaks | 致命錯誤（rc 1、無報告） | ok、0 項 | error + 原因 |
| npm audit | lockfile 損毀 | error | 不變（原本就正確） |

### 〔DevSecOps〕
- [x] 新狀態 `ToolStatus.INCOMPLETE`＋`ToolResult.skipped`（上限 50）；`_execute` 以回傳值 `Partial` 帶出，不存在共用的 adapter 單例上
- [x] MCP `get_scan_result`：未完成工具顯示 pending／running（原本佔位值顯示 ok）；incomplete 附 `not_analysed`
- [x] 摘要新增 `tools_incomplete`；UI 新狀態「未完整」（琥珀色）＋提示；中英文字串
- [x] 以新 adapter 在容器內對真實掃描器驗證：bearer／trivy／osv（部分）→ incomplete，gitleaks → error，正常專案 → ok，osv 全壞 → not_applicable
- [x] 無新增相依、無抑制註解

### 〔QA〕
- [x] pytest 344 → 363（新增 19，輸入皆取自 VM 實測輸出）；JS 測試通過

### 關卡
- [x] G1 → [x] G2 → [x] G3（實測驗證；人工檢視：skipped 內容以 textContent 呈現、長度有上限） → [x] G4（363/363）
- [x] G5：CI run 36141303808 綠燈；報表判讀：程式掃描皆 0，trivy-image High 199 與前兩版相同（既有）
- [x] G6：.145 fetch 成功、快轉至 e8a3ff3，deploy.sh DEPLOY_RC=0、health 200
- [x] 上線實測（scan f3f7e51a48f1，nessus-report ea47db3）：進行中工具顯示 running（不再是 ok）；bearer → incomplete（renderer/app.js 逾時）；判定 manual_review 並列出缺口；tools_run 5、tools_incomplete 1。修正前同一情況會顯示 passed、0 項

### 已知風險／待決
- [x] **判定規則納入工具狀態**（使用者同意，talk.md #021）：任一工具 error／timeout／incomplete 時，原本 passed 改為 manual_review，並在判定、MCP、畫面標頭列出缺口；blocked 不變；unavailable／not_applicable 不算缺口。README 中英同步；測試 363 → 375
- bearer 在這台 VM 上目前無法完整分析 99 KB 的 JS 檔（閒置也一樣），現在會照實顯示 incomplete；根治需要更多 CPU 或新版 bearer
- 既有問題：adapter 是全域單例，`scan()` 把 custom_rules／_on_stage 寫在 self 上，兩個同時進行、自訂規則不同的掃描可能互相覆蓋（本輪未修）

## 本輪：MCP 掃描地端檔案（兩段式 ZIP 上傳）（2026-09-25，第 40 輪）

使用者需求：MCP 目前只能掃公開 git URL，無法掃未推送的地端程式碼；希望沿用網頁的 ZIP 上傳。使用者選定「兩段式上傳」（talk.md #018）。

### 〔PM〕評估與設計
- [x] 對照 CoreMain／talk.md #014：git URL 限制不變；新路徑與網頁 ZIP 上傳對等，不給助理更多能力（不開放本機路徑）
- [x] Exit Criteria：票證一次性、10 分鐘、綁帳號、不與 API 權杖互通、不進網址／日誌；沿用 ZIP 流程所有限制；測試覆蓋每條限制

### 〔DevSecOps〕編寫與資安確認
- [x] `app/mcp.py`：新工具 `create_upload_scan`；票證 `sastup_` + 256-bit、sha256 存記憶體、鎖內 pop 防重用；每帳號 5 張、全域 50 張上限
- [x] `app/main.py`：`PUT /api/mcp/upload`（閘道精確比對列為公開、以票證驗證、Origin 檢查、重查帳號停用與密碼過期、先寫暫存再建 job、串流大小上限、殘檔 1 小時清理）；MCP 回傳的 upload_url 用 `_public_origin`（防偽造標頭）
- [x] 獨立複核（security-scanner 子代理）：Critical/High/Medium 0、Low 3 → FIND-001（匿名共用配額）、FIND-002（殘檔）已修並有測試；FIND-003 見已知風險
- [x] 無新增相依、無抑制註解

### 〔QA〕品質測試
- [x] pytest 323 → 344 全數通過（新增 21：端對端、一次性、無效票證×4、API 權杖不可當票證、過期、帳號停用、雜湊存放、配額、匿名配額、超量×2、空檔、zip-slip、外站 Origin、跨帳號讀取、檔名清理、殘檔清理）
- [x] 突變測試：移除串流大小檢查 → 僅 chunked 案例失敗，證明兩條檢查路徑各自被覆蓋

### 關卡
- [x] G1 → [x] G2 → [x] G3（人工複核；自動 SAST 待 CI） → [x] G4（344/344）
- [x] G5 CI：run 36136296454 綠燈（Tests + Full security scan）；下載報表逐項判讀：semgrep/bearer/gitleaks/pip-audit/trivy-fs/osv 皆 0；trivy-image High 199 與前一版 cf00f7e（198）相同，來自 Debian 基底與內附 gitleaks 二進位，非本輪引入
- [x] G6 CD：.145 fetch 成功後快轉至 1a712fe，scripts/deploy.sh DEPLOY_RC=0、healthy、health 200
- [x] 上線實測：MCP create_upload_scan → curl PUT ZIP 201 → get_scan_result done；同票證重用 401

### 已知風險
- 映像 trivy-image High 199（既有，多數無修補版本）：另開一輪評估基底映像更新
- ZIP 內若把專案包在子目錄，npm_audit 判定不適用、**bearer 回報 0 且狀態 ok（實際未分析）**；打包時檔案放根目錄
- [ ] **bearer 在 git 掃描少報**：nessus-report 67016f3 經 git URL 掃描 bearer 1 項、經 ZIP 掃描 10 項（scan fd384992869e vs 000f69fa05ff）。需找出根因（clone 後的 .git？bearer 的 git 感知行為？），並讓「分析了 0 個檔案」不再顯示為 ok
- FIND-003 [Low]：同時進行中的上傳數量無上限（網頁上傳原本即如此；此路徑多一個入口），磁碟最壞為 併發數 × MAX_UPLOAD_BYTES
- 票證存記憶體：服務重啟即失效（呼叫者重新申請即可）；若日後改為多 worker 需改共享儲存

## 本輪：README 優化（繁中完整說明）（2026-09-23，第 39 輪）

使用者需求：README 附專案照片、繁體中文、說明各掃描器會上傳哪些資料、詳述部署／更新維護腳本、詳述 API。

### 〔PM〕評估與設計
- [x] 對照 CoreMain：純文件改善，不新增功能，不偏離主題
- [x] 範圍：`README.zh.md` 全面改寫；`README.md` 只加截圖與中文連結、修正錯誤 API 列（最小改動）
- [x] 保留測試要求的字串（`git clone` 在 `./setup.sh --docker` 之前、`SKIP_DEPLOY`、`--rollback`、「掃描記錄例外」）

### 〔DevSecOps〕編寫與資安確認
- [x] 以六工具實跑示範專案擷取 7 張截圖至 `docs/images/`（不含真實憑證；示範密鑰為公開範例值）
- [x] 逐一讀 adapter 參數，整理每個工具的對外連線與傳出資料（Semgrep metrics off、npm/OSV 傳套件名稱版本、Gitleaks 離線…）
- [x] 依 `app/main.py` 全部路由重寫 API 章節（權限、參數、狀態碼、回應欄位、CI 範例、MCP）
- [x] 修正舊文件與程式不符處：判定標記已改存伺服器、維護動作已限管理員、`/api/scans/{id}/review`、`/exceptions` 不存在

### 〔QA〕品質測試
- [x] 以 API 權杖實測 `/api/scans`、`/api/scans/{id}`、`/api/inspect`、`/mcp tools/list`、`tools/call`，確認文件範例正確
- [x] `pytest -q` 全數通過（含 README 內容檢查測試）

### 已知風險（本輪發現，未修）
- 監控分頁「維護動作」UI 文字仍寫「沒有登入保護」，但後端已 `require_admin`；屬 UI 文案過時，留待下一輪。
- `SAST_SEMGREP_RULES` 已無程式讀取（掃描改用 `SAST_SEMGREP_RULESETS`），但 compose／.env.example 仍設定它。
- 英文 README 其他段落（例如判定標記存在瀏覽器）仍有過時描述，以中文版為準。

## 本輪：`--update` 改用本機快取（2026-09-21，第 34 輪）

### 〔PM〕範圍
- [x] 根因：`install-tools.sh` 每次重新下載，沒有使用 `fetch-vendor.sh` 已建立的 `vendor/` 快取
- [x] 對照 CoreMain：屬現有部署流程修正，**不新增機制**，只把兩個斷開的腳本接起來

### 〔DevSecOps〕實作
- [x] `download()` 加快取分支（唯一入口，不碰各個 install_* 函式）
- [x] `--update` 先呼叫 `fetch-vendor.sh`，`SKIP_VENDOR=1` 可退出
- [x] FIND-001：未驗證的下載不可寫入 `vendor/`（快取會被後續視為已驗證）
- [x] 雜湊只留一份：新增 `fetch-vendor.sh --expected-for`

### 〔QA〕驗證
- [x] 258 條測試全數通過（新增 8 條）
- [x] 變異測試：破壞快取檔名、拿掉快取填充、寫入損壞檔 —— 均被測試捕捉
- [x] VM 實測：三支工具從快取安裝 **120 ms**（原約 30 分鐘）
- [x] VM 實測：安裝後版本正確且 checksum 符合

### 關卡
- [x] G1 需求與範圍確認（使用者直接提出）
- [x] G2 功能完成、語法檢查通過
- [x] G3 資安：FIND-001 已修並有測試覆蓋
- [x] G4 品質：258 條通過 + VM 實測
- [x] G5 CI 綠燈（Tests + Full security scan）
- [ ] G6 CD：**尚未重建映像** —— VM 容器內仍為 osv-scanner 1.9.2

### 已知風險
- [ ] 第 33 輪的 osv-scanner 2.6.0 升級**尚未進入容器**（上次 build 被中斷）。
      主機上已是 2.6.0，但實際掃描跑在容器內。
- [ ] bearer 未納入 `vendor/` 快取（未釘版本，沒有發布 checksum），
      因此 `--update` 仍會為它下載一次。

## 本輪：設定分頁重構 + 密碼原則 + 角色可見性（2026-09-20，第 24 輪）

### 〔PM〕範圍
- [x] 設定分頁改為使用者指定的階層：帳號／MCP／登入紀錄／工具矩陣
- [x] 密碼原則可設定：複雜度、閒置登出、禁止重複使用、密碼到期
- [x] 角色可見性：管理員看全部分頁；一般使用者只看掃描、報告
- [x] MCP 範本可下載，含 .env 範本
- [x] 工具矩陣：移除「可否自訂規則」，改為「是否對外連線 + 連線用途」
- [x] 重新整理後停留在原分頁
- [x] 對照 CoreMain：密碼原則屬「帳號系統」既有範圍，非新主題；角色可見性是第 23 輪
      已定義的兩種角色的自然延伸，**不擴充為細緻權限矩陣**

### 〔DevSecOps〕實作
- [x] `accounts.py`：密碼歷史表、原則設定表、閒置逾時、到期判定
- [x] `POST /api/auth/policy`（僅管理員），超出範圍伺服器端拒絕
- [x] 角色可見性：后端已有 require_admin，前端依角色隱藏分頁
- [x] 工具矩陣改為連線說明（數字皆於 VM 實測，非抄文件）
- [x] 分頁狀態存 localStorage 並於載入時還原（還原前先過角色闘門）
- [x] 設定頁四組子分頁，一次只顯示一組

### 〔QA〕驗證
- [x] 密碼重用被拒、到期強制改密碼、閒置逾時登出（第 24 輪前段已驗）
- [x] 一般使用者看不到監控分頁（前端隱藏）且后端回 403／404（VM 實測）
- [x] 重新整理保持分頁
- [x] 178 條測試全數通過（新增 5 條）
- [x] 新增測試已反轉行為驗證其會失敗（不是空綠燈）

### 關卡
- [x] G1：需求與使用者確認（四層階層 + 分頁記憶）
- [x] G2：功能完成、語法檢查通過
- [x] G3：資安——原則寫入僅管理員（VM 實測 403），前端隱藏僅為遠飾
- [x] G4：178 條通過
- [x] G5：CI run 35493501492 綠燈
- [x] G6：已部署至 VM（7d65b67），healthy，簽入後驗證全數通過

### 已知風險（本輪未做，刻意不擴張範圍）
- [ ] 登入尚未做速率限制（rate limiting）——密碼猜測只被複雜度擋下
- [ ] API token 無到期機制，只能手動撤銷
- [ ] MCP 掃描無配額，一組 token 可無限次觸發掃描
- [ ] 一般使用者仍可見設定分頁（只能改自己密碼、管自己 token），
      這是刻意的：否則他們沒有任何地方可以變更密碼

## 本輪：登入系統、API token、MCP（2026-09-20，第 23 輪）
- [x] 設定分頁：我的帳號（改密碼／登出）、API token、使用者管理
- [x] 登入系統：scrypt（標準庫，零新相依）、session、最後一位管理員不可移除
- [x] API token 與使用者綁定：停用帳號 → token 立即失效（實測）
- [x] MCP 伺服器：Streamable HTTP、三個工具、token 驗證、Origin 驗證
- [x] 所有 Semgrep 規則集改為預設啟用
- [x] 部署腳本補 `.env` 建立與首次管理員密碼提示
- [x] **CoreMain.md 第 22 行原訂「不做帳號系統」已註明修訂與理由**
- [x] 資安複驗 Critical 2 / High 5 全部修復並逐項實證：
  - [x] 維運／本機路徑／規則寫入改 require_admin（403）
  - [x] 掃描結果綁 owner，非擁有者 404（REST 與 MCP 都修）
  - [x] SSRF：擋 metadata／loopback／IPv6／私有網段
  - [x] CSRF：補伺服器端 Origin 驗證
  - [x] Secure cookie：改讀 X-Forwarded-Proto
  - [x] 登入時間差 2x → 1.00x（帳號列舉）
  - [x] 改密碼一併撤銷 API token
- [x] 測試 126 → 165；CI 綠燈；部署 0b46f0b 並驗證
- [ ] 未做（複驗建議、非阻斷）：登入速率限制、token 有效期、MCP 掃描配額

## 本輪：移除人工審查、合併規則選擇（2026-09-19，第 20 輪）
- [x] 移除人工審查表單；同時移除 `/api/scans/{id}/review` 與 `JobManager.review()`
- [x] 保留「需人員審查」判定（使用者選定），讓 PDF 上印得出來
- [x] **修正我上一輪引入的缺陷**：分頁導致列印只抓前 100 筆，1343 筆會靜默少掉 1243 筆
- [x] 匯出 PDF 前先渲染全部；列印樣式隱藏「顯示更多」、判定改黑白高對比
- [x] 合併「Semgrep 規則集」與「自訂規則」兩個清單（使用者指出重複）
- [x] 一個清單兩組：公開規則集（Semgrep 官方）／你自己的規則
- [x] 測試 113 → 115；CI 綠燈；部署 b911d69 驗證通過
- [x] G1～G6 全過

## 本輪：大量結果的畫面修正、規則集多選、掃描進度條（2026-09-19，第 19 輪）
> 使用者掃 3181 檔、1343 筆發現後暴露的問題。

### 〔PM〕範圍與決策
- [x] 問題 1 根因：`renderBlockedBox` 把**全部** blocking_findings 列出（本次 1066 筆），淹沒真正的發現卡片
- [x] 決策：整個移除該區塊（使用者選定）——下方完整列表已有相同資訊
- [x] 問題 2：Semgrep 規則集改為多選清單，預設 p/default + p/owasp-top-ten
- [x] 已知限制：`--config auto` 與 `--metrics=off` 衝突（第 15 輪實證），不列入選項
- [x] 問題 3 澄清：Trivy **不執行**被掃描的程式碼；先前的 Critical 是指 Rego 規則本身是程式語言
- [x] 問題 4：進度條依各工具實際流程分階段顯示

### 〔DevSecOps〕實作
- [x] 移除 renderBlockedBox（1066 筆阻擋項淹沒畫面的根因）
- [x] 後端 SEMGREP_RULESETS 可多選；adapter 對每個 ruleset 加一次 --config（聯集非取代）
- [x] 新增 /api/rulesets；只接受已發布的 id，拒絕任意路徑與 URL
- [x] 前端：掃描頁規則集勾選 UI，預設 p/default + p/owasp-top-ten
- [x] ToolResult 新增 stage；base 在 probe→applicability→execute 三點回報
- [x] 各工具宣告自己的 first_stage：semgrep=rules / trivy=vulndb / osv,npm=advisories / gitleaks=secrets / bearer=dataflow
- [x] 發現列表改為每頁 100 筆 + 顯示更多（1343 筆一次塞 DOM 會卡頓）

### 〔QA〕驗證
- [x] 測試 107 → 113（多規則集展開、未知 ruleset 被丟棄、階段順序、階段失敗不影響掃描）
- [x] VM 實測：階段確實顯示（trivy=vulndb 持續數輪，正是先前看起來像當掉的那段）
- [x] VM 實測：/api/rulesets 不提供 auto；預設兩個規則集生效

### 關卡
- [x] G1 → [x] G2 → [x] G3 → [x] G4（113/113）
- [x] G5（CI run 35448814869 綠燈） → [x] G6（部署 9645250，healthy、6/6 工具）

## 本輪：自訂規則編輯器（2026-09-19，第 18 輪）
> 掃描頁右側新增規則編輯器，讓使用者寫／存／選自訂規則，儲存前驗證可用。

### 〔PM〕範圍與決策
- [x] 可行性已實證：semgrep 自訂 YAML 與 trivy 自訂 Rego 都在容器內跑通
- [x] 驗證指令確認：`semgrep validate <file>`（good=0 / bad=2）；trivy 以試跑驗證
- [x] 決策：規則存 Docker volume `/data/rules`（不進版控，重啟不遺失）
- [x] 決策：可複選——預設規則集 + 勾選的自訂規則同時套用
- [ ] 範圍：僅 Semgrep（YAML）與 Trivy（Rego）；其餘四工具不做
- [ ] 明確不做：規則版本歷史、共享／匯入匯出、語法高亮編輯器（用原生 textarea）

### 〔DevSecOps〕實作
- [x] `app/rules.py`：規則 CRUD + 驗證（slug 白名單、副檔名由伺服器決定、resolve 後比對 parent）
- [x] 內建範本（唯讀）：semgrep 2 個、trivy 1 個，都是「本來就跑得起來」的完整範例
- [x] API 五個端點：列表 / 讀取 / 驗證 / 儲存 / 刪除
- [x] adapter 接自訂規則：semgrep `--config` 可重複；trivy `--config-check` + `--check-namespaces`
- [x] 前端：掃描頁右側編輯器（選 → 編輯 → 檢查 → 儲存 → 勾選啟用），版面改雙欄
- [x] 掃描開始時複製規則進工作區，避免掃描中改規則影響進行中的掃描

### 〔DevSecOps〕資安修補（security-scanner 獨立複驗 6 項）
- [x] **FIND-001 Critical**：Trivy Rego 可呼叫 `http.send`，**實測真的打出 HTTP 並拿到 200**
- [x] 修法：規則執行前以 denylist 擋掉 http.send / net.lookup_ip_addr / opa.runtime / rego.parse_module / trace
- [x] 實測原始 exploit 現在被拒，錯誤訊息說明原因
- [x] FIND-002 High：驗證端點加 Semaphore(2)、timeout 120→60 秒
- [x] FIND-003 High：加每 engine 100 條上限；rules 與 workspaces 改回兩個獨立 volume
- [x] FIND-004 Medium：semgrep 加 `--timeout 30 --timeout-threshold 3` 防 ReDoS
- [x] FIND-005 Medium：改 temp file + `os.replace` 原子寫入
- [x] FIND-006 Low：`$` 會匹配結尾換行 → 改 `\Z`

### 〔QA〕驗證
- [x] 測試 92 → 107（路徑穿越 11 種、Rego denylist 9 種、原子寫入、數量上限）
- [x] VM 實測：內建範本列出、好規則存檔(201)、壞規則被拒(含行號)、http.send 被拒(400)
- [x] VM 實測：自訂規則在真實掃描中命中，且預設規則集同時生效（確認是「加上去」不是「取代」）
- [x] VM 實測：規則在映像重建後仍存在（volume 持久化生效）

### 關卡
- [x] G1 → [x] G2 → [x] G3（Critical/High 皆修復並實證） → [x] G4（107/107）
- [x] G5（CI 綠燈，掃描器全 0） → [x] G6（部署 c3ce71d，healthy、6/6 工具）

## 本輪：移除可設定政策，改為固定判定規則（2026-09-19，第 17 輪）
- [x] 判定規則固定：Critical/High 或密鑰 → 不可上線；Medium → 人員審查；Low/Info/零發現 → 通過
- [x] 使用者確認：零發現算「通過」
- [x] 移除政策範本選單、六個規則核取方塊、保存天數、PR/Release 宣告
- [x] 移除誤報例外機制（使用者決定）；blocked 改為列出發現並說明「修好再掃」
- [x] 掃描頁改為一行規則說明，位於開始掃描按鈕上方
- [x] 更新結果分類：upgraded / data_updated / already_current，只有 upgraded 提示需重啟
- [x] 進度條摺疊：114MB 下載從數十行收斂成一行
- [x] i18n 刪除 76 個不再使用的鍵；移除 16 行死 CSS
- [x] 測試 61 → 70（新增 9 組參數化判定階梯 + 密鑰不分等級 + 簽章防護）
- [x] 修正 test_csv_export 競態（本機過、CI 掛：背景執行緒未等終態）
- [x] CI 綠燈、gitleaks 0；部署 b9dc410，實測 High → blocked
- [x] 淨減 426 行程式碼

## 本輪：監控分頁強化 + 部署加速（2026-09-19，第 16 輪）
> 使用者四項需求：Docker 效能可視、手動更新掃描器/重啟、部署先下載再建置、CI/CD 驗證上線。

### 〔PM〕範圍與決策
- [ ] 需求 1：監控分頁看得到 Docker 效能（docker_stats 模組已存在，缺的是「預設啟用 + 自動化部署設定」）
- [ ] 需求 2：監控頁手動觸發掃描器更新與服務重啟
- [ ] 需求 3：部署改為「先下載到主機快取 → build 直接用」，解決 75 KB/s 網路下重建過久
- [ ] 需求 4：CI/CD 驗證並部署到 VM
- [x] 決策：admin 動作**不加開關、直接開放**（使用者明示決定，已知無認證保護，記為已知風險）
- [x] 決策：重啟 = 重啟應用程序（reload FastAPI），不碰 docker.sock 寫入權限
- [x] 決策：快取策略 = 主機先下載四個二進位 → Dockerfile 從本地 COPY

### 〔DevSecOps〕實作
- [x] docker-compose 預設開啟 SAST_ENABLE_DOCKER_STATS 並掛載 docker.sock:ro
- [x] **實測才發現**：光掛載不夠。socket 是 root:988，容器跑 appuser(1000) → Permission denied
- [x] 解法 `group_add: ["${DOCKER_GID}"]`；gid 每台主機不同，由 deploy.sh 自動偵測寫入 .env
- [x] 後端 POST /api/admin/update-tools（非阻塞、回傳工作狀態）
- [x] 後端 POST /api/admin/restart（延遲退出讓回應送達，由 restart policy 拉起）
- [x] 前端監控頁：Docker 效能卡片 + 更新/重啟按鈕 + 進行中狀態 + 日誌
- [x] Dockerfile：支援 vendor/ 本地快取，缺檔時回退網路下載
- [x] scripts/fetch-vendor.sh：可續傳 + SHA256 校驗（上游官方值）
- [x] scripts/deploy.sh：快取→建置→切換→健康檢查→可回滾，一鍵完成
- [x] 文件標註：README 中英明列 admin 端點無認證的風險與 nginx 網段限制選項

### 〔DevSecOps〕資安修補（security-scanner 兩輪複驗）
- [x] FIND-001 High 重啟 DoS：加入 60 秒節流
- [x] FIND-002 High 日誌洩漏：_scrub() 在寫入端遮罩憑證與絕對路徑
- [x] FIND-003 Med TOCTOU：重啟在同一鎖臨界區內宣告 job slot
- [x] FIND-004 Med 主機資產洩漏：容器清單依 compose label 限縮
- [x] FIND-005 Med 供應鏈：三個二進位比對上游官方 SHA256（osv 無官方檔，已註明較弱佐證）
- [x] FIND-006 Low 狀態卡死：try/finally 確保釋放 job slot
- [x] FIND-007 Low npm 必失敗：移除就地更新路徑
- [x] FIND-008 Low 遮罩殘留：補 Bearer/netrc 憑證與 /usr/local/bin、/etc、/var、/tmp 路徑
- [x] FIND-009 Med（**我自己引入的**）重啟佔住 job slot 無路徑歸還：加 RESTART_GRACE 自我復原

### 〔QA〕驗證
- [x] 測試 37 → 61 項全通過
- [x] 測試：admin 端點正常路徑、併發防護、錯誤處理、POST-only
- [x] 測試：遮罩 11 種洩漏樣式（參數化）且不破壞正常輸出
- [x] **VM 實測發現節流無效**：last_restart_at 放記憶體，重啟本身就清掉它 → 改存檔案
- [x] VM 實測：Docker 效能顯示正常（2 容器 CPU/記憶體）
- [x] VM 實測：更新工具成功（semgrep + trivy DB）、併發被拒 409、日誌零洩漏
- [x] VM 實測：重啟成功（pid 改變、服務 healthy）
- [x] VM 實測：竄改快取檔 → checksum 正確攔截並重抓
- [x] 量測：重建 138s（先前多次 40 分鐘以上且失敗）；完整部署 446s

### 關卡
- [x] G1 範圍確認 → [x] G2 實作完成 → [x] G3 資安（High=0，兩輪複驗）
- [x] G4 QA（61/61） → [x] G5 CI（run 35436911014 綠燈，gitleaks 0）
- [x] G6 CD（部署 aa30f32，healthy、0 restarts、6/6 工具可用）

## 本輪：CD 部署到 VM + 修復 Semgrep 規則集衝突（2026-09-19，第 15 輪）
- [x] 推送 3 個未推送的 UI commit，CI run 35403653584 綠燈
- [x] 下載 artifact 判讀：原始碼面六項全數 0；映像層 11 CRITICAL 與 G5 基線相同，未新增曝險
- [x] 部署前把線上映像標記 `sast-studio:rollback-30ef0e8`（可一行回滾）
- [x] 先 build 再 `up -d` 切換，服務中斷僅數秒
- [x] Smoke test：UI 200、/api/tools、/api/policies（新端點，證明新版真的上線）、404 防呆
- [x] E2E：上傳 zip → 確認閘門 → 掃描完成 → 政策判定 passed
- [x] **修復既有缺陷**：Semgrep `auto` 規則集與 `--metrics=off` 衝突，導致每次掃描必失敗
- [x] 改為 `p/default`（保留 `--metrics=off` 隱私設定），同步修 config/compose/.env.example/README 中英
- [x] 新增回歸測試，並實測還原 `auto` 會讓測試失敗
- [x] 修正後重新部署 `0c36ebc` 並複驗：`tools_run` 由 1 → 2，semgrep status=ok
- [x] 複驗抓到 CWE-78 OS Command Injection，政策正確判定 manual_review（政策引擎線上實證）
- [x] 六個工具在線上全部 available；四個端點皆 200；healthy、0 restarts
- [x] 清理 VM 上的測試產物；保留 rollback 映像標籤與 compose 備份
- [ ] 待辦：CI workflow 把掃描器失敗降為 warning，使這類「工具根本沒跑起來」的缺陷長期無人察覺，建議下一輪區分「掃到漏洞」（警告）與「工具沒跑起來」（擋 build）

## 本輪：UI 改版 — 分頁重構、政策說明、報告匯出、Docker 資源評估（2026-09-18，第 14 輪）
- [x] 拆成三分頁：掃描 / 報告 / 監控
- [x] 政策說明重寫：每條規則改為「當任一工具發現 X → 判定為 Y」，並明示會產生該類發現的工具
- [x] 明講政策套用在「全部工具彙整後」的結果，不屬於單一工具（使用者最主要的困惑）
- [x] 明講「判定」只是結論標記，不會中止掃描或阻止部署（原本叫「一律阻擋」名不符實）
- [x] 顯示每條規則在本次掃描的實際命中數
- [x] 報告頁：左列歷次掃描（判定＋嚴重度計數＋時間），右側細節
- [x] 報告頁上方嚴重度分佈條狀圖 + 圖例
- [x] CSV 匯出（後端標準庫 csv，零新相依）＋ 防試算表公式注入（CWE-1236）
- [x] PDF 匯出（瀏覽器原生列印 + @media print 樣式，零新相依）
- [x] 監控頁新增「資源評估」：記憶體對照上限、未設限制示警、OOM/限流實證、CPU 忙碌僅提示
- [x] 政策範本文案改為語言中立，中英各自在地化
- [ ] 未做：政策判定尚未提供 CI 可消費的 exit code／verdict 端點（本輪刻意不擴張範圍）

## 本輪：CI 驗證 G5（2026-09-18，第 13 輪）
- [x] 推送 main 觸發 CI：run 35350129044、35350605149 皆綠燈（Tests + Full security scan）
- [x] 下載 artifact 逐項判讀（綠燈≠乾淨：workflow 刻意把掃描器失敗降為 warning）
- [x] 原始碼面：semgrep / pip-audit / osv-scanner / trivy-fs / gitleaks / bearer 全數 0
- [x] 映像層 11 個 CRITICAL 已歸屬：0 個在本專案程式碼與 Python 相依，全在 Debian base image（fix=none）與內嵌掃描器二進位
- [x] Dockerfile：semgrep 之後重新升級 setuptools/msgpack（msgpack 實測 1.1.2→1.2.2）
- [x] 查明 Trivy 回報的 setuptools 70.3.0 / msgpack 1.1.2 為 base image SBOM 中繼資料（FilePath=None），非實體檔案
- [ ] 映像 base image 與內嵌掃描器弱點：隨上游版本更新持續追蹤（非本專案可控）

## 本輪：掃描政策（Policy Gate）收尾（2026-09-18，第 12 輪）
> 承接工作區既有的未提交變更（policies.py + 政策 UI），以修正與補齊為主，不擴張範圍。
- [x] 修正自訂政策被丟棄的缺陷：`new_job()` 改收已解析的 PolicyDefinition，不再用 id 反查（原本送出自訂規則必 500）
- [x] 補 zh 字典缺少的 `status.policy_review` / `status.blocked`（原本中文介面顯示原始鍵）
- [x] 政策 UI 全面改用 `t()` + `data-i18n`，不再硬寫「英文 / 中文」雙語字串
- [x] 政策範本名稱與說明在前端在地化（後端維持語言中立，沿用第 7 輪準則）
- [x] 移除例外端點中兩個分支完全相同的死碼
- [x] 補上誤報例外的 UI（後端端點原本沒有任何前端入口）
- [x] 修正輪詢終止清單漏掉 policy_review / blocked（原本會無限輪詢）
- [x] 修正進度條在 policy_review / blocked 時消失（工具其實已跑完）
- [x] 語言切換時重繪政策表單與下拉選項
- [x] 判定結果以顏色區分（passed 綠 / manual_review 橘 / blocked 紅）
- [x] 補測試：自訂規則端到端、blocked→例外→解除、例外過期後重新生效、review 狀態防呆
- [x] 補 README.zh.md 的政策章節與 API 列（原本中文文件完全沒提到政策）

## 上一輪安全優化（2026-09-17）
- [x] 將 `python-multipart` 安全版本下限提升至 `0.0.31` 以上。
- [x] 升級 Docker 與 Ubuntu 安裝流程的 `pip/setuptools/wheel`。
- [x] 建置 Docker 映像前更新 Debian 套件，並加入容器 `HEALTHCHECK`。
- [x] 重新執行測試、Docker build 與完整 CI 安全掃描。
- [ ] Bearer 回報的 1 Critical / 3 High 保留人工複核，不以忽略規則掩蓋。

## 範圍與假設
- 範圍：六個工具 adapter、orchestrator（job 管理 + 平行執行 + 結果正規化）、單頁 Web UI、三種掃描輸入（zip 上傳 / 本機路徑 / Git URL），以及 Ubuntu/Docker 的自動安裝入口。明確不做：帳號系統、自動修補、歷史資料庫、任何攻擊工具。
- 假設：工具不一定都安裝在環境中 → 採 adapter + 可用性偵測 + 優雅降級；docker-compose 提供「一次裝齊」的佈建選項。
- 技術棧 / 環境：Python 3.11 + FastAPI（後端）、原生 HTML/CSS/JS（前端）、Ubuntu + Docker Engine/Compose。
- 測試環境存取：Ubuntu 安裝路徑使用 apt、Python venv、Node/npm、git 與 Docker Compose；Docker 路徑提供完整六工具映像。

## 上線標準（Exit Criteria，三維度合一）
- 功能：六個 adapter 皆能正確回報可用性、可用時能執行並正規化、不可用時優雅降級；三種輸入皆可建立掃描；UI 能建立掃描、看進度、看彙整結果；Ubuntu 與 Docker 安裝入口能建立可驗證的執行環境。
- 資安：本體無命令注入（全 list 參數 shell=False）、無 Zip Slip/Bomb、Git URL 受限 http/https、secret 遮罩；本體 SAST 自檢 Critical/High = 0；無硬編碼密鑰。
- 品質：單元/整合測試通過率 100%、Critical/Major 缺陷 = 0；核心流程（可用性偵測、正規化、輸入安全、orchestrator 狀態機）皆有測試。

## 〔PM〕評估與設計
- [x] 確認專案主題、建立 `CoreMain.md`
- [x] 需求釐清與可行性評估（五工具本質 / 依賴 / 執行方式已釐清）
- [x] 架構設計與使用者確認（FastAPI + adapter + docker-compose + 三種輸入；Docker 授權疑慮已澄清=伺服器免費）
- [x] 訂出範圍、驗收標準、Exit Criteria

## 〔DevSecOps〕編寫與資安確認
- [x] 共用資料模型（Severity / Finding / ToolResult / Job）
- [x] BaseAdapter：可用性偵測 / 適用性判斷 / 執行 / 正規化 介面
- [x] SemgrepAdapter（JSON 輸出解析、severity/CWE/OWASP 映射）
- [x] ~~CodeQLAdapter~~ → 移除（授權費用）；改用下列兩個免費工具取代
- [x] BearerAdapter（免費語意 SAST，最接近 CodeQL，按 severity 分組 JSON 解析）
- [x] TrivyAdapter（免費全能：vuln + secret + IaC misconfig，Results JSON 解析）
- [x] NpmAuditAdapter（package.json 適用、npm audit --json 解析）
- [x] OsvScannerAdapter（lockfile 掃描、OSV JSON 解析）
- [x] GitleaksAdapter（密鑰偵測、secret 遮罩）
- [x] 輸入來源處理 source.py（zip 防 slip/bomb、路徑驗證、Git URL 驗證 + clone）
- [x] Orchestrator / JobManager（記憶體 job store、ThreadPool 平行執行、狀態機）
- [x] FastAPI 路由（建立掃描 / 查詢 / 列表 / 工具狀態 / 靜態前端）
- [x] 前端 UI（選目標/工具、輪詢進度、彙整報表 + 篩選）
- [x] docker-compose + Dockerfile（一次裝齊六工具）
- [x] SAST/SCA 自檢 + 人工審查邏輯層（見 lessons.md 本輪紀錄）

## 〔QA〕品質測試
- [x] TC 可用性偵測（工具不存在時回 unavailable + install_hint）
- [x] TC 正規化（各工具樣本輸出 → 統一 Finding）
- [x] TC 輸入安全（Zip Slip 阻擋、Git URL scheme 驗證、路徑驗證）
- [x] TC orchestrator 狀態機（queued→running→done、彙整 summary）
- [x] TC API（工具狀態、建立掃描、查詢、404）
- [x] TC secret 遮罩

## 〔CI/CD〕交付與部署
- [x] 驗證目標：GitHub Actions（使用者選定推 main 觸發）
- [x] CI workflow 範本（lint/test/self-SAST gate）已附於 repo
- [x] CD：已部署至單台 VM 192.168.99.145:8080（docker-compose），保留 rollback 映像標籤

## 〔本輪〕自動安裝腳本補強
- [x] Ubuntu `setup.sh`：系統前置檢查、venv、Python 相依、掃描器安裝、測試驗證
- [x] Docker Compose 作為完整六工具安裝路徑
- [x] native scanner installer 逐項繼續、原子下載、最終列出缺少工具

## 關卡狀態（由編排器維護）
- [x] G1 PM→Dev：設計確認、Exit Criteria 訂定
- [x] G2 Dev→Sec：功能完成、單元自測通過
- [x] G3 Sec→QA：資安 Exit Criteria 達標
- [x] G4 QA→結案：品質 Exit Criteria 達標
- [x] G5 結案→CI：CI 綠燈（run 35350605149）；原始碼面掃描器全數 0，映像層發現已歸屬並判讀
- [x] G6 CI→CD：已部署至 VM 192.168.99.145（docker-compose），健康檢查 healthy、0 restarts，smoke + E2E 通過

## 已知風險
- 本機路徑掃描（ALLOW_LOCAL_PATH）能掃伺服器任意可讀路徑，屬 operator 權限功能，預設開啟並於文件標註風險。
- npm audit / OSV-Scanner / Trivy（首次下載弱點 DB）需外網；離線環境會回 error/unavailable（已優雅處理）。
- 工具間有重疊（Trivy 與 OSV 都做 SCA、Trivy 與 Gitleaks 都做 secret）；本輪不做跨工具去重，屬可接受（不同引擎覆蓋不同），去重列為日後增強。

---

## 本輪：.240 乾淨重建驗證 Docker 監控（2026-09-23，第 15 輪）

### 目標
在 VM 192.168.99.240 上走**真正的首次部署路徑**，驗證 Docker 效能監控
會自動啟用——不是靠既有容器的殘留狀態蒙混過關。

### 背景（為什麼需要這一輪）
第 14 輪修了 `setup.sh --docker` 的順序 bug：它直接 `compose up`，
從未寫過 `DOCKER_GID`，所以全新機器的第一個容器拿到 999 fallback，
監控永遠是 permission denied。修法已 push，但**只在 .240 上用
`deploy.sh` 驗證過修復效果，沒有驗證過真正的首次部署路徑**。

### 範圍與假設
- 範圍：.240 移除容器 + 刪 .env → `./setup.sh --docker` → 驗證監控自動啟用
- 明確不做：不刪具名 volume（`down` 不加 `-v`）、不改程式碼
- 環境：VM 192.168.99.240，SSH 可達（已確認）；憑證不寫入本檔
- 假設：主機 docker.sock gid=988（已量測確認）

### Exit Criteria
- 功能：`setup.sh --docker` 一路跑完不中斷；服務回 200
- 資安：不新增弱點（本輪不改碼，沿用 G5 基線）
- 品質：容器 GroupAdd = 988（非 999）；`docker_stats.collect()`
  available=True 且 cpu/mem 有實數；背景取樣器有在跑；
  三個具名 volume 存活

### 〔PM〕評估與設計
- [x] 確認主題對照 CoreMain（部署驗證，不新增功能，不偏離主軸）
- [x] 取得可實際測試的環境（.240 SSH 已通）
- [x] 與使用者確認驗證強度（選：乾淨重建）
- [x] 訂出 Exit Criteria

### 〔DevSecOps〕部署
- [x] 盤點現況（commit 986c2fe / 2 容器 / 3 volume / .env=988）
- [x] 乾淨重建：compose down（未加 -v）+ rm .env 完成
- [x] 已確認：.env 不存在、容器列表為空
- [x] `./setup.sh --docker` 一路跑完（commit 2cf0cc4）

### 〔QA〕驗證
- [x] TC-01 PASS：/api/health 200、/login 200
- [x] TC-02 PASS：GroupAdd=[988]，容器內 groups 含 988（非 999）
- [x] TC-03 PASS：available=True，兩容器 cpu/mem 皆為實數
- [x] TC-04 PASS：無人看管下自動記錄 3 筆狀態變化（含 error 級）
- [x] TC-05 PASS：accounts/rules/workspaces 全程存活
- [x] TC-06 PASS：五類別齊備、保留 30 天

### 關卡狀態
- [x] G1 PM→Dev：設計確認、Exit Criteria 訂定、使用者確認驗證強度
- [x] G6 CI→CD：.240 首次部署完成，Docker 監控自動啟用並經實證

---

## 本輪：設定分頁加入 Docker 規格參數（2026-09-23，第 16 輪）

### 目標
在「設定」分頁讓使用者看到目前的容器資源規格、實際用量與主機總量，
填入想要的規格後**產生一段 compose 設定供複製**，並附上套用指令。

### 背景
capacity 目前對兩個容器都回 `warn: mem_unlimited`——compose 沒設上限，
失控的掃描可能吃光主機記憶體。使用者看得到警告，卻沒有地方可以調整。

### 關鍵設計決策（實測後定案）
實測：容器透過掛載的 docker socket **有** `/containers/update` 的寫入權限，
`:ro` 只讓 socket 檔案唯讀，不限制透過它下的指令（回應 409 非 403）。

**決定：不使用該權限。** 應用程式只讀，產生設定檔讓使用者自己套用。
理由：
- 直接寫入會讓應用從「能讀容器狀態」升級為「能改主機上任何容器」，
  一旦被攻破影響範圍大很多（OWASP A01 權限控制、最小權限原則）
- compose 才是真相來源；繞過它改的值，下次 deploy 就被覆蓋，
  使用者會以為設定掉了
- 經使用者確認採此方案

### 範圍與假設
- 範圍：設定分頁新增「Docker 規格」區塊（唯讀顯示 + 產生設定 + 複製）
- 明確不做：不呼叫 /containers/update、不自動改 compose 檔、不自動重啟
- 假設：docker socket 已掛載（未掛載時要有明確的降級顯示）

### Exit Criteria
- 功能：顯示現有上限/用量/主機總量；產生的 compose 片段語法正確
- 資安：不新增任何寫入類 docker API 呼叫；端點需登入；非管理員看不到
- 品質：測試涵蓋「無 socket」「無上限」「已有上限」三種狀態；311+ 全過

### 〔PM〕評估與設計
- [x] 確認主題對照 CoreMain（監控既有主軸的延伸，不偏離）
- [x] 與使用者確認範圍（只讀 + 產生設定檔）
- [x] 訂出 Exit Criteria

### 〔DevSecOps〕實作
- [x] 後端：limits() 讀 limits/用量/主機總量
- [x] 後端：compose_fragment() 含伺服器端驗證
- [x] 前端：設定 -> Docker 規格 分頁
- [x] i18n 中英雙語（各 19 鍵）
- [x] SAST 自我掃描通過（CI Full security scan 綠燈）

### 〔QA〕測試
- [x] TC-01 PASS：availability=False 時回降級訊息不拋錯
- [x] TC-02 PASS：.240 實測兩容器皆標示無上限 ⚠
- [x] TC-03 PASS：.240 產生片段 YAML 正確
- [x] TC-04 PASS：5 種不合理輸入全數擋下（含 YAML 注入）
- [x] TC-05 PASS：兩端點未登入皆回 401
- [x] TC-06 PASS：323 測試全過；CSS 遺漏已修並加守門測試

### 關卡狀態
- [x] G1 PM→Dev：設計確認、Exit Criteria 訂定、使用者確認方案
- [x] G2 Dev→Sec：功能完成、自測通過
- [x] G3 Sec→QA：無寫入類 docker API、端點需管理員
- [x] G4 QA→結案：TC-01~06 全 PASS（TC-06 曾退回修 CSS）
- [x] G5 結案→CI：CI 綠燈（測試 + 完整資安掃描）
- [x] G6 CI→CD：.240 部署完成，healthy、restarts=0
