# 待修改（Secure SDLC 計畫）

## 目標
建立一個統一 Web 應用「SAST Studio」，把 Semgrep、Bearer、Trivy、npm audit、OSV-Scanner、Gitleaks 六個安全掃描工具整合成前端 UI + 後端 orchestrator，讓使用者一鍵對程式碼跑多工具靜態安全檢查並看彙整報表。

## 本輪：監控分頁強化 + 部署加速（2026-09-19，第 16 輪）
> 使用者四項需求：Docker 效能可視、手動更新掃描器/重啟、部署先下載再建置、CI/CD 驗證上線。

### 〔PM〕範圍與決策
- [ ] 需求 1：監控分頁看得到 Docker 效能（docker_stats 模組已存在，缺的是「預設啟用 + 自動化部署設定」）
- [ ] 需求 2：監控頁手動觸發掃描器更新與服務重啟
- [ ] 需求 3：部署改為「先下載到主機快取 → build 直接用」，解決 75 KB/s 網路下重建過久
- [ ] 需求 4：CI/CD 驗證並部署到 VM
- [x] 決策：admin 動作**不加開關、直接開放**（使用者明示決定，已知無認證保護，記為已知風險）
- [x] 決策：重啟 = 重啟應用程序（reload FastAPI），不碰 docker.sock 寫入權限
- [x] 決策：快取策略 = 主機先下載四個二進位 → Dockerfile 從本地 COPY

### 〔DevSecOps〕實作
- [ ] docker-compose 預設開啟 SAST_ENABLE_DOCKER_STATS 並掛載 docker.sock:ro
- [ ] 後端 POST /api/admin/update-tools（非阻塞、回傳工作狀態）
- [ ] 後端 POST /api/admin/restart（延遲退出讓回應送達，由 restart policy 拉起）
- [ ] 前端監控頁：Docker 效能卡片 + 更新/重啟按鈕 + 進行中狀態
- [ ] Dockerfile：支援 vendor/ 本地快取，缺檔時回退網路下載
- [ ] 部署腳本 deploy.sh：預先下載四個二進位（可續傳、校驗）再 build
- [ ] 文件標註：admin 端點無認證保護的風險

### 〔QA〕驗證
- [ ] 測試：admin 端點正常路徑、併發防護、錯誤處理
- [ ] 測試：docker stats 停用時優雅降級
- [ ] VM 實測：Docker 效能顯示、更新工具、重啟服務
- [ ] 量測：部署時間改善幅度

### 關卡
- [ ] G1 範圍確認 → [ ] G2 實作完成 → [ ] G3 資安 → [ ] G4 QA → [ ] G5 CI → [ ] G6 CD

## 本輪：CD 部署到 VM + 修復 Semgrep 規則集衝突（2026-09-19，第 15 輪）
- [x] 推送 3 個未推送的 UI commit，CI run 35403653584 綠燈
- [x] 下載 artifact 判讀：原始碼面六項全數 0；映像層 11 CRITICAL 與 G5 基線相同，未新增曝險
- [x] 部署前把線上映像標記 `sast-studio:rollback-30ef0e8`（可一行回滾）
- [x] 先 build 再 `up -d` 切換，服務中斷僅數秒
- [x] Smoke test：UI 200、/api/tools、/api/policies（新端點，證明新版真的上線）、404 防呆
- [x] E2E：上傳 zip → 確認閘門 → 掃描完成 → 政策判定 passed
- [x] **修復既有缺陷**：Semgrep `auto` 規則集與 `--metrics=off` 衝突，導致每次掃描必失敗
- [x] 改為 `p/default`（保留 `--metrics=off` 隱私設定），同步修 config/compose/.env.example/README 中英
- [x] 新增回歸測試，並實測還原 `auto` 會讓測試失敗
- [x] 修正後重新部署 `0c36ebc` 並複驗：`tools_run` 由 1 → 2，semgrep status=ok
- [x] 複驗抓到 CWE-78 OS Command Injection，政策正確判定 manual_review（政策引擎線上實證）
- [x] 六個工具在線上全部 available；四個端點皆 200；healthy、0 restarts
- [x] 清理 VM 上的測試產物；保留 rollback 映像標籤與 compose 備份
- [ ] 待辦：CI workflow 把掃描器失敗降為 warning，使這類「工具根本沒跑起來」的缺陷長期無人察覺，建議下一輪區分「掃到漏洞」（警告）與「工具沒跑起來」（擋 build）

## 本輪：UI 改版 — 分頁重構、政策說明、報告匯出、Docker 資源評估（2026-09-18，第 14 輪）
- [x] 拆成三分頁：掃描 / 報告 / 監控
- [x] 政策說明重寫：每條規則改為「當任一工具發現 X → 判定為 Y」，並明示會產生該類發現的工具
- [x] 明講政策套用在「全部工具彙整後」的結果，不屬於單一工具（使用者最主要的困惑）
- [x] 明講「判定」只是結論標記，不會中止掃描或阻止部署（原本叫「一律阻擋」名不符實）
- [x] 顯示每條規則在本次掃描的實際命中數
- [x] 報告頁：左列歷次掃描（判定＋嚴重度計數＋時間），右側細節
- [x] 報告頁上方嚴重度分佈條狀圖 + 圖例
- [x] CSV 匯出（後端標準庫 csv，零新相依）＋ 防試算表公式注入（CWE-1236）
- [x] PDF 匯出（瀏覽器原生列印 + @media print 樣式，零新相依）
- [x] 監控頁新增「資源評估」：記憶體對照上限、未設限制示警、OOM/限流實證、CPU 忙碌僅提示
- [x] 政策範本文案改為語言中立，中英各自在地化
- [ ] 未做：政策判定尚未提供 CI 可消費的 exit code／verdict 端點（本輪刻意不擴張範圍）

## 本輪：CI 驗證 G5（2026-09-18，第 13 輪）
- [x] 推送 main 觸發 CI：run 35350129044、35350605149 皆綠燈（Tests + Full security scan）
- [x] 下載 artifact 逐項判讀（綠燈≠乾淨：workflow 刻意把掃描器失敗降為 warning）
- [x] 原始碼面：semgrep / pip-audit / osv-scanner / trivy-fs / gitleaks / bearer 全數 0
- [x] 映像層 11 個 CRITICAL 已歸屬：0 個在本專案程式碼與 Python 相依，全在 Debian base image（fix=none）與內嵌掃描器二進位
- [x] Dockerfile：semgrep 之後重新升級 setuptools/msgpack（msgpack 實測 1.1.2→1.2.2）
- [x] 查明 Trivy 回報的 setuptools 70.3.0 / msgpack 1.1.2 為 base image SBOM 中繼資料（FilePath=None），非實體檔案
- [ ] 映像 base image 與內嵌掃描器弱點：隨上游版本更新持續追蹤（非本專案可控）

## 本輪：掃描政策（Policy Gate）收尾（2026-09-18，第 12 輪）
> 承接工作區既有的未提交變更（policies.py + 政策 UI），以修正與補齊為主，不擴張範圍。
- [x] 修正自訂政策被丟棄的缺陷：`new_job()` 改收已解析的 PolicyDefinition，不再用 id 反查（原本送出自訂規則必 500）
- [x] 補 zh 字典缺少的 `status.policy_review` / `status.blocked`（原本中文介面顯示原始鍵）
- [x] 政策 UI 全面改用 `t()` + `data-i18n`，不再硬寫「英文 / 中文」雙語字串
- [x] 政策範本名稱與說明在前端在地化（後端維持語言中立，沿用第 7 輪準則）
- [x] 移除例外端點中兩個分支完全相同的死碼
- [x] 補上誤報例外的 UI（後端端點原本沒有任何前端入口）
- [x] 修正輪詢終止清單漏掉 policy_review / blocked（原本會無限輪詢）
- [x] 修正進度條在 policy_review / blocked 時消失（工具其實已跑完）
- [x] 語言切換時重繪政策表單與下拉選項
- [x] 判定結果以顏色區分（passed 綠 / manual_review 橘 / blocked 紅）
- [x] 補測試：自訂規則端到端、blocked→例外→解除、例外過期後重新生效、review 狀態防呆
- [x] 補 README.zh.md 的政策章節與 API 列（原本中文文件完全沒提到政策）

## 上一輪安全優化（2026-09-17）
- [x] 將 `python-multipart` 安全版本下限提升至 `0.0.31` 以上。
- [x] 升級 Docker 與 Ubuntu 安裝流程的 `pip/setuptools/wheel`。
- [x] 建置 Docker 映像前更新 Debian 套件，並加入容器 `HEALTHCHECK`。
- [x] 重新執行測試、Docker build 與完整 CI 安全掃描。
- [ ] Bearer 回報的 1 Critical / 3 High 保留人工複核，不以忽略規則掩蓋。

## 範圍與假設
- 範圍：六個工具 adapter、orchestrator（job 管理 + 平行執行 + 結果正規化）、單頁 Web UI、三種掃描輸入（zip 上傳 / 本機路徑 / Git URL），以及 Ubuntu/Docker 的自動安裝入口。明確不做：帳號系統、自動修補、歷史資料庫、任何攻擊工具。
- 假設：工具不一定都安裝在環境中 → 採 adapter + 可用性偵測 + 優雅降級；docker-compose 提供「一次裝齊」的佈建選項。
- 技術棧 / 環境：Python 3.11 + FastAPI（後端）、原生 HTML/CSS/JS（前端）、Ubuntu + Docker Engine/Compose。
- 測試環境存取：Ubuntu 安裝路徑使用 apt、Python venv、Node/npm、git 與 Docker Compose；Docker 路徑提供完整六工具映像。

## 上線標準（Exit Criteria，三維度合一）
- 功能：六個 adapter 皆能正確回報可用性、可用時能執行並正規化、不可用時優雅降級；三種輸入皆可建立掃描；UI 能建立掃描、看進度、看彙整結果；Ubuntu 與 Docker 安裝入口能建立可驗證的執行環境。
- 資安：本體無命令注入（全 list 參數 shell=False）、無 Zip Slip/Bomb、Git URL 受限 http/https、secret 遮罩；本體 SAST 自檢 Critical/High = 0；無硬編碼密鑰。
- 品質：單元/整合測試通過率 100%、Critical/Major 缺陷 = 0；核心流程（可用性偵測、正規化、輸入安全、orchestrator 狀態機）皆有測試。

## 〔PM〕評估與設計
- [x] 確認專案主題、建立 `CoreMain.md`
- [x] 需求釐清與可行性評估（五工具本質 / 依賴 / 執行方式已釐清）
- [x] 架構設計與使用者確認（FastAPI + adapter + docker-compose + 三種輸入；Docker 授權疑慮已澄清=伺服器免費）
- [x] 訂出範圍、驗收標準、Exit Criteria

## 〔DevSecOps〕編寫與資安確認
- [x] 共用資料模型（Severity / Finding / ToolResult / Job）
- [x] BaseAdapter：可用性偵測 / 適用性判斷 / 執行 / 正規化 介面
- [x] SemgrepAdapter（JSON 輸出解析、severity/CWE/OWASP 映射）
- [x] ~~CodeQLAdapter~~ → 移除（授權費用）；改用下列兩個免費工具取代
- [x] BearerAdapter（免費語意 SAST，最接近 CodeQL，按 severity 分組 JSON 解析）
- [x] TrivyAdapter（免費全能：vuln + secret + IaC misconfig，Results JSON 解析）
- [x] NpmAuditAdapter（package.json 適用、npm audit --json 解析）
- [x] OsvScannerAdapter（lockfile 掃描、OSV JSON 解析）
- [x] GitleaksAdapter（密鑰偵測、secret 遮罩）
- [x] 輸入來源處理 source.py（zip 防 slip/bomb、路徑驗證、Git URL 驗證 + clone）
- [x] Orchestrator / JobManager（記憶體 job store、ThreadPool 平行執行、狀態機）
- [x] FastAPI 路由（建立掃描 / 查詢 / 列表 / 工具狀態 / 靜態前端）
- [x] 前端 UI（選目標/工具、輪詢進度、彙整報表 + 篩選）
- [x] docker-compose + Dockerfile（一次裝齊六工具）
- [x] SAST/SCA 自檢 + 人工審查邏輯層（見 lessons.md 本輪紀錄）

## 〔QA〕品質測試
- [x] TC 可用性偵測（工具不存在時回 unavailable + install_hint）
- [x] TC 正規化（各工具樣本輸出 → 統一 Finding）
- [x] TC 輸入安全（Zip Slip 阻擋、Git URL scheme 驗證、路徑驗證）
- [x] TC orchestrator 狀態機（queued→running→done、彙整 summary）
- [x] TC API（工具狀態、建立掃描、查詢、404）
- [x] TC secret 遮罩

## 〔CI/CD〕交付與部署
- [x] 驗證目標：GitHub Actions（使用者選定推 main 觸發）
- [x] CI workflow 範本（lint/test/self-SAST gate）已附於 repo
- [x] CD：已部署至單台 VM 192.168.99.145:8080（docker-compose），保留 rollback 映像標籤

## 〔本輪〕自動安裝腳本補強
- [x] Ubuntu `setup.sh`：系統前置檢查、venv、Python 相依、掃描器安裝、測試驗證
- [x] Docker Compose 作為完整六工具安裝路徑
- [x] native scanner installer 逐項繼續、原子下載、最終列出缺少工具

## 關卡狀態（由編排器維護）
- [x] G1 PM→Dev：設計確認、Exit Criteria 訂定
- [x] G2 Dev→Sec：功能完成、單元自測通過
- [x] G3 Sec→QA：資安 Exit Criteria 達標
- [x] G4 QA→結案：品質 Exit Criteria 達標
- [x] G5 結案→CI：CI 綠燈（run 35350605149）；原始碼面掃描器全數 0，映像層發現已歸屬並判讀
- [x] G6 CI→CD：已部署至 VM 192.168.99.145（docker-compose），健康檢查 healthy、0 restarts，smoke + E2E 通過

## 已知風險
- 本機路徑掃描（ALLOW_LOCAL_PATH）能掃伺服器任意可讀路徑，屬 operator 權限功能，預設開啟並於文件標註風險。
- npm audit / OSV-Scanner / Trivy（首次下載弱點 DB）需外網；離線環境會回 error/unavailable（已優雅處理）。
- 工具間有重疊（Trivy 與 OSV 都做 SCA、Trivy 與 Gitleaks 都做 secret）；本輪不做跨工具去重，屬可接受（不同引擎覆蓋不同），去重列為日後增強。
