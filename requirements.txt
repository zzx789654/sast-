# Minimum versions, not pins. A pin would freeze the security fixes these
# floors exist to pull in; CI runs pip-audit against whatever resolves.
#
# Each floor below is here because something below it is vulnerable. Raising
# one is a security change, not housekeeping.
fastapi>=0.111
uvicorn[standard]>=0.30
# 0.0.31+ fixes the multipart parser DoS and parameter-smuggling advisories.
python-multipart>=0.0.31
# httpx is used by fastapi's TestClient in the test suite.
httpx>=0.27

# Transitive, floored here so a resolver cannot pick a vulnerable one:
#   anyio  < 4.14.2  GHSA-82r6-8w77-94w6 (TLS cert spoofing via IDNA 2003),
#                    GHSA-5p39-cfhj-2xmp (worker blocks on undrained stderr)
#   idna   < 3.15    GHSA-65pc-fj4g-8rjx / CVE-2026-45409 (encode() bypasses
#                    the CVE-2024-3651 fix; quadratic on crafted input)
# The running image already resolves above both; without these lines a fresh
# resolve is free to pick an older one, which is what a scan reported.
anyio>=4.14.2
idna>=3.15
