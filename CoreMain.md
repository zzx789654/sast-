# CoreMain — 專案中心思想（North Star）

> 本檔由 PM 在專案最開始、與使用者確認後建立，是整個專案的「中心思想」。
> 之後所有功能開發、測試、驗收都以本檔為中心：圍繞主題、維持簡單順手不複雜。

## 一句話專案主題（Theme）
一個統一的 Web 應用，把 **Semgrep、CodeQL、npm audit、OSV-Scanner、Gitleaks** 五個安全掃描工具整合在同一個介面下，讓使用者對一份程式碼「一鍵跑完多種靜態安全檢查」，並在同一張報表看到彙整後的弱點結果。

## 核心價值（North Star）
- 使用者用它最想得到的那一件事：**不必分別安裝、記指令、各自看報表**，在一個網頁上選目標、選工具、按下掃描，就能拿到跨工具彙整的安全發現。
- 成功長什麼樣（一句話）：貼一個 Git URL 或上傳一包程式碼，幾分鐘後看到一份依嚴重度排序、標好工具來源與 CWE/OWASP 的統一弱點清單。

## 範圍主軸（圍繞主題）
- 圍繞主題會做：
  - 五個工具各一個 **adapter**（可用性偵測 + 執行 + 結果正規化）。
  - 一個 **orchestrator**（接收掃描目標、挑選適用工具、平行執行、彙整結果）。
  - 一個 **Web UI**（選目標/工具、看進度、看彙整報表、依嚴重度/工具/檔案篩選）。
  - 掃描目標輸入：**上傳 zip、指定本機路徑、貼 Git URL clone**。
- 與主題無關、明確不做（防 scope creep）：
  - 不做使用者帳號系統、多租戶、權限角色管理。
  - 不做弱點的自動修補（auto-fix）或 PR 生成。
  - 不寫任何攻擊工具 / exploit —— 只做防禦性靜態檢查。
  - 不做長期資料庫儲存歷史（先用記憶體 job store；歷史留待日後）。

## 設計原則（所有功能一律遵守）
- **簡單**：後端一個 FastAPI app，工具用統一的 adapter 介面；前端單頁、原生 JS，不引入前端框架。
- **順手**：一鍵掃描、輪詢看進度、結果一頁看完；工具沒裝就明講並附安裝指引，不讓使用者卡住。
- **不複雜**：每個 adapter 只負責「偵測 / 執行 / 正規化」一件事；orchestrator 不理解各工具細節，只呼叫統一介面。
- **取捨準則**：「功能更強」與「更簡單順手」衝突時，預設選簡單順手。以「輪詢」而非 WebSocket、以「記憶體 job store」而非資料庫，都是刻意選簡單。

## 主題對照檢核（每加一個功能前先自問）
- [ ] 這個功能是在服務「用一個網頁統一跑五個安全工具」這個主題嗎？
- [ ] 有沒有更簡單、更順手的做法達成同樣目的？
- [ ] 它會不會讓整體變複雜、偏離主軸？

## 安全紀律（本工具自身的安全，最高優先）
> 這是一個「跑別人程式碼的安全工具」，本體若不安全就是笑話。
- 所有外部程式一律 `subprocess` **list 參數 + shell=False + timeout**，永不字串拼接進 shell。
- zip 解壓縮防 **Zip Slip / Zip Bomb**（路徑逃逸、檔案數/解壓大小上限）。
- Git URL 只允許 http/https，clone 時關閉互動提示，加 timeout。
- Gitleaks 掃到的密鑰值一律**遮罩**後才回傳，不在 UI 外洩真實 secret。
- 每個掃描 job 在獨立工作區執行，結束即清理。
